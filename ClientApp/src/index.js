
import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App"

//root element is where comps will be render
const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
    <App/>
)